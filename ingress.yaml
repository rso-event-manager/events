apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: events-ingress
spec:
  rules:
    - http:
        paths:
          - path: /events(/|$)(.*)
            backend:
              serviceName: events
              servicePort: 80
