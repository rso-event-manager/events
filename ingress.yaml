apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: events-ingress
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /$2
spec:
  rules:
    - http:
        paths:
          - path: /(.*)
            backend:
              serviceName: events
              servicePort: 80
